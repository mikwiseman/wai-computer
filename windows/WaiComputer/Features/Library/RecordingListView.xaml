<Page
    x:Class="WaiComputer.Features.Library.RecordingListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid RowDefinitions="Auto,*">
        <Grid Grid.Row="0" Padding="24,16" ColumnDefinitions="*,Auto">
            <TextBlock Grid.Column="0" x:Name="TitleLabel" Text="All recordings" Style="{ThemeResource TitleTextBlockStyle}" />
            <Button Grid.Column="1" Content="New" Style="{ThemeResource AccentButtonStyle}" Click="OnNew" />
        </Grid>
        <ListView Grid.Row="1" x:Name="ItemsList" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="12">
                        <TextBlock Text="{Binding Title}" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                        <TextBlock Text="{Binding Subtitle}" Style="{ThemeResource CaptionTextBlockStyle}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
