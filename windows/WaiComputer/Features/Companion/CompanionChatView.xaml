<Page
    x:Class="WaiComputer.Features.Companion.CompanionChatView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24" RowDefinitions="*,Auto">
        <ScrollViewer Grid.Row="0">
            <ItemsRepeater x:Name="MessagesRepeater" />
        </ScrollViewer>
        <Grid Grid.Row="1" ColumnDefinitions="*,Auto" Margin="0,12,0,0">
            <TextBox Grid.Column="0" x:Name="InputBox" PlaceholderText="Ask anything…" AcceptsReturn="True" />
            <Button Grid.Column="1" Content="Send" Style="{ThemeResource AccentButtonStyle}" Click="OnSend" />
        </Grid>
    </Grid>
</Page>
