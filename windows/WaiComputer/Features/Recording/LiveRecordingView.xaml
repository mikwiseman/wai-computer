<Page
    x:Class="WaiComputer.Features.Recording.LiveRecordingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24" RowDefinitions="Auto,*,Auto">
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16">
            <Ellipse Width="14" Height="14" Fill="{ThemeResource SystemFillColorCriticalBrush}" />
            <TextBlock x:Name="DurationLabel" Text="00:00" Style="{ThemeResource SubtitleTextBlockStyle}" />
            <TextBlock x:Name="SourceLabel" Text="Mic + system audio" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollMode="Auto" Margin="0,16,0,16">
            <ItemsRepeater x:Name="Segments">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource ControlFillColorDefaultBrush}" CornerRadius="8" Padding="12" Margin="0,4">
                            <StackPanel>
                                <TextBlock Text="{Binding Speaker}" Style="{ThemeResource CaptionTextBlockStyle}" />
                                <TextBlock Text="{Binding Text}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
            <Button Content="Pause" Click="OnPause" />
            <Button Content="Stop" Style="{ThemeResource AccentButtonStyle}" Click="OnStop" />
        </StackPanel>
    </Grid>
</Page>
