# Materials + Brain upgrade — plan & progress

Branch: `feat/sb-materials-brain` (worktree `/Users/mikwiseman/Documents/Code/wai-mat-brain`, off `origin/main`).
Migrations chain off head **`20260601_130000_memory_proposals`**.
Full audit/research/roadmap JSON: `/private/tmp/claude-501/-Users-mikwiseman-Documents-Code-wai-computer/569b3edf-f67e-42f2-9351-0cddbfd2c083/tasks/wyzg3788q.output`

## Keystone insight
The Brain viz is not the real blocker — **the knowledge graph is data-empty in prod**: `extract_entities()` has zero callers, `EntityRelation` is built nowhere, `entity.embedding` is never written, and `EntityRelation` has no `item_id`. Any graph/wiki UI built before the graph is populated renders empty (current Mac failure mode). Order: **fix → fill → visualize**.

## Confirmed decisions (Mik, 2026-06-01)
- **Brain default view:** Index → ego/local graph. Tri-view: Index (default) / Wiki / Graph.
- **Graph nodes:** entities **+ items** as nodes (true Obsidian), with a toggle to hide item nodes.
- **Surface order:** web **and** Mac **in parallel**.
- **Video by link:** direct-media-URL (.mp4/Vimeo/Loom) → Deepgram now; YouTube captions now; **yt-dlp deferred** (opt-in + daily cap, Phase 5).
- Uploaded video/audio → **Recording** pipeline (reuse diarization/status/trash); items get a pointer later if needed.
- Storage: **local-disk behind a swappable interface**; hold 200MB cap with an honest "share a link" refusal.
- Entity merge: exact normalized-name upsert only; fuzzy/cosine → human-confirm Review queue (never silent-merge).
- Web graph lib: `react-force-graph-2d` (≤2k nodes; sigma.js+graphology upgrade path documented). Mac graph: SwiftUI Canvas + capped Verlet.
- OCR (scanned PDF) + browser/Share extension: **deferred** (separate builds).

## Phases (ordered by leverage)

### Phase 0 — Quick wins + no-fallbacks compliance
- [x] Backend: derived `status` (fetching|summarizing|ready|needs_input|failed) + `error{code,message}` in `ItemResponse`/`ItemListEntry`. (commit 01ccea77)
- [x] Backend: kill silent enqueue swallow in `items.py` (state=failed + processing_error on broker fail). (01ccea77)
- [x] Backend: kill silent enqueue swallow in `comparisons.py` (mark set failed, drop dead intent block). (01ccea77)
- [x] Backend: race-safe idempotency in `ingest_item` (SAVEPOINT + IntegrityError → re-SELECT). (01ccea77)
- [x] Backend: GIN FTS index on `item_chunks.content` (migration 20260601_140000, validated on scratch DB). (01ccea77)
- [x] Web: onCreated add→feed fix (reloadKey signal replaces loadRecordingsState); status-driven badges + error tooltips; poll-while-pending + focus refresh; real API error in AddAnything/ItemDetail; `status`/`error` TS types.
- [~] Web+Mac taxonomy + `pdf` chip: WEB + Mac `pdf` chips done; WaiComputerKit single-source taxonomy (labels+icons) still pending (Phase 1).
- [x] Mac: errorMessage inline banner in Content feed; `MacBrainView try? getBrain()` → do/catch + error/Retry (distinct from empty); `addDraft()` background poll; pdf chip. (xcodebuild ✓)

### Phase 1 — File ingestion: the missing entrance
> SEQUENCING (2026-06-01): document upload (1a/b/c) is DONE across backend+web+Mac = the core of goal #1. The remaining Phase 1 items (media-upload async, `classify_url` media, video allow-list, needs_input reprocess UI, SSRF guard) are DEFERRED to a pre-launch hardening pass — SSRF isn't urgent pre-v1 (no other users). **Pivoting to Phase 2 (graph keystone) next** to unblock the flagship Brain viz.
- [x] Backend: `POST /items/upload` — DOCUMENTS (PDF/txt/md → extract text → Item; 200MB cap; UUID storage key; dedup-unlink; shared `_enqueue_item_summary`) **and MEDIA** (audio/video → streamed to staging → `import_uploaded_media_task` Celery task → `import_media_as_recording` ffmpeg-normalise + transcribe → Recording; 202 processing). One "add anything" endpoint, routed by type. Broker-down → 503 (file cleaned up); corrupt → Sentry. SSRF guard on the URL-fetch path is still a separate slice.
  - IMPL NOTE: build a NEW `/items/upload` route — do NOT overload `/recordings/{id}/upload` (recordings.py:2853), it's intricate (claim-race `_claim_audio_upload`, size-mismatch, `_mark_recording_failed`). Reuse `_stage_upload_to_disk` (recordings.py:987) + `MAX_UPLOAD_SIZE` (2850) + `_measure_upload_size` (860). PDF→`source_fetch._extract_pdf_text` (or pdfplumber) → `ingest_item`; audio/video → `recording_import.import_media_as_recording` (recording_import.py:456, handles video via `_normalize_media_for_transcription`→Deepgram). Storage key `{user_id}/{uuid}.{ext}` — never trust `UploadFile.filename`.
- [x] Backend: media-file upload handled via `/items/upload` → `import_media_as_recording` (no need to widen `recordings.py` — that task is audio-only/no video normalise; the items route owns the ffmpeg path). Recordings endpoint left untouched.
- [ ] Backend: `classify_url` for direct media (.mp4/Vimeo/Loom) + reroute audio/* video/* content-type to transcription.
- [ ] Backend: `needs_input` reprocess endpoint (append body / attach file + re-enqueue).
- [~] Web: AddAnythingPanel drag-drop + Attach-file → `uploadItem` (native HTML5, no new dep) — DONE. PENDING: paste-detect type chip before submit, multi-file queue, per-file progress bar.
- [x] Mac: `.dropDestination` + `.fileImporter` (PDF/text/markdown) in Content feed; `APIClient.uploadItem(fileURL:folderId:title:)` streaming multipart-from-temp-file; VM `uploadFile` + background poll. (swift test 473 ✓, xcodebuild ✓)
- [ ] Web+Mac: render `needs_input` recovery (why + Paste/Attach/Retry).

### Phase 2 — Populate the knowledge graph (keystone)
- [x] Backend: recording-pipeline entity seeding — `apply_summary_result` (live/regenerate) + `_persist_summary` (import) seed people/topics → entities + mentions (cheap, zero extra LLM), mirroring items. DEFERRED enrichment (gated/nightly, not inline): the richer `extract_entities` LLM pass for typed relations (works_on/reports_to) + `entity.embedding` for similarity edges.
- [x] Backend: item-pipeline entity seeding — `generate_item_summary` promotes people_mentioned/topics → entities + mentions (cheap, zero extra LLM) via `app/core/entity_graph.py` (`upsert_entity` exact-dedup + IntegrityError race-safe, `record_mention`, `seed_entities_from_summary`).
- [x] Backend: polymorphic `EntityMention(user_id, entity_id, source_kind, source_id, chunk_id?, context, weight)` table + `entities(user_id,type,name)` unique constraint — migration 20260602_100000 (validated on scratch DB).
- [~] Backend: feed Items into nightly consolidator (`_gather_user_material`) — DEFERRED enrichment (not blocking Phase 3; makes items compound into durable memory blocks).

### Phase 3 — Brain visualization on web (flagship)
- [x] Backend: `GET /api/brain/graph` — entities + item/recording source nodes; mention + co-occurrence edges; `?focus=` ego graph; `limit` degree cap; honest empty graph. `core/brain_graph.py` + 5 tests. (similarity edges deferred — need `entity.embedding` from the gated LLM pass.)
- [x] Backend: wiki — `GET /api/entities/{id}/page` (`build_entity_page`): source backlinks (items/recordings with titles + context) + co-occurrence related entities ranked by shared sources + mention_count; 404 for non-owned. 4 tests; suite 95.13%.
- [x] Web: Brain section — nav (Library → Brain) + **Index** + **Graph** (force-directed) + Index/Graph tab switcher. Graph = `react-force-graph-2d@1.29.1` (dynamic `ssr:false`, color-by-kind, size-by-degree, tooltip, click entity→backend ego-focus + Reset, show-sources toggle, explicit canvas height, no-fallback error/retry). **BROWSER-VERIFIED LIVE** (logged in, seeded 6 entities/3 items/9 mentions, screenshotted Index + force graph). PENDING (deferred polish): Wiki tab (`GET /api/entities/{id}/page`), graph `zoomToFit` centering on engine-stop.

### Phase 4 — Brain on Mac + comparison hardening + power features
- [~] Mac Brain: KIT (4a) + `MacBrainView` tabbed **Index/Wiki** (4b) DONE. Index = categorized entity rows (people/topics/projects from `getBrainGraph`, degree-sorted, tap→Wiki). Wiki = the tapped entity's page (`getEntityPage`: infobox + tappable Related + Sources backlinks with context). Honest empty/error+Retry; mounted in MacContentView. xcodebuild ✓. PENDING: Graph tab (4c — SwiftUI Canvas + capped force sim).
- [~] Backend: comparison correctness — DONE: `intent` column (migration 20260602_110000) persisted+echoed; create-time de-dupe + ≥2-distinct (422); `POST /comparisons/{id}/rebuild` recovery; **deleted-source filter** (`deleted_at IS NULL`; fail explicitly if <2 survive, else build survivors + note the exclusion — no silent row-drop); **Celery retry** on transient errors (max_retries=3 + `is_retryable_exception` guard, mirrors embedding_backfill). export md/csv (`GET /{id}/export?format=md|csv`, activates `to_markdown` + a CSV writer) + cell-edit (`PATCH /{id}`, sets the `edited` flag) DONE. PENDING (perf, low-pri — Celery retry already prevents "lose everything"): parallel row extraction w/ per-row failure tolerance.
- [~] Web power features: **unified search DONE** — new "Everything" mode → `/api/search/all`, spans recordings **and** items, rendered as a unified hit list (source-kind badge + snippet + score; open→recording detail or jump to Content). PENDING: comparison-builder UI (multi-select items → `createComparison`), connect-a-source UI.

### Phase 5 — Robustness + governance
- [ ] Tiered video-by-link / yt-dlp (opt-in + daily cap); media classify_url; simhash fuzzy dedup; governance for items+entities; Person↔Entity reconcile; Brain-health lint; optional OCR (docling, MIT only).

## Progress log
- 2026-06-01: worktree + analysis workflow (21 agents) done; decisions locked; starting Phase 0 backend.
- 2026-06-01: Phase 0 COMPLETE across surfaces — backend (01ccea77), web (fa061364), Mac (58bda2f7, xcodebuild ✓).
- 2026-06-01: Phase 1a — `POST /items/upload` for documents (PDF/txt/md → Item). 19 items-route tests green; full backend suite 95.11% (gate ✓). Heads-up: main is red on 2 `test_observability.py` tests (`OPS_FORWARD_GENERIC_ERRORS` default-off, commit c0661d4b — owned by the sentry-telegram-bridge loop, NOT this work). Next: media upload (audio/video→Recording, async), `classify_url` media + SSRF guard, then web drag-drop + Mac .fileImporter.
- 2026-06-01: Phase 1b — web AddAnythingPanel drag-drop + Attach-file → `uploadItem` (native HTML5, no dep). 8 AddAnything tests green; eslint + tsc clean. Next: Mac `.fileImporter` + `APIClient.uploadItem`, then media-upload async routing.
- 2026-06-01: Phase 1c — Mac upload (kit `APIClient.uploadItem` + `.fileImporter`/drop in Content feed + VM `uploadFile`). swift test 473 ✓; xcodebuild ✓. **Document upload (PDF/txt/md) now works web AND Mac, end-to-end.** MEDIA-UPLOAD design recon: reuse `import_media_as_recording` (creates the Recording, normalizes video, transcribes) from a NEW Celery task that reads staged bytes — recordings' own `process_staged_recording_upload` is audio-only (no video normalize).
- 2026-06-02: Phase 2a — knowledge-graph FOUNDATION. `EntityMention` polymorphic table + `entities` dedup constraint (migration 20260602_100000, scratch-DB validated); `app/core/entity_graph.py` (upsert/mention/seed, exact-dedup + race-safe); item-pipeline seeding so **items now create person/topic entities + mentions at zero extra LLM cost** — the graph finally gets data. +3 entity-graph tests, item_summary test extended; full suite 95.10% (gate ✓). 2 pre-existing observability failures still unrelated. Next: wire recording-pipeline `extract_entities` (LLM pass — sets `entity.embedding` + EntityRelation edges) + feed items to the consolidator, then Phase 3 (`GET /api/brain/graph` + web Brain viz).
- 2026-06-02: Phase 2b — recording-pipeline entity seeding (cheap path) wired into `apply_summary_result` + `_persist_summary`, mirroring items. **Graph now has data from BOTH recordings and items** (people/topics → entities + mentions), zero extra LLM cost. test_summary_generation_core extended; full suite 95.10% (gate ✓). Phase 2 graph-DATA done (LLM extract_entities pass + consolidator items DEFERRED as gated enrichment). **Pivoting to Phase 3 — the flagship: `GET /api/brain/graph` (nodes+edges: mention + co-occurrence + similarity) then the web Brain tri-view (Index/Wiki/Graph, react-force-graph-2d, ego default, items toggle).**
- 2026-06-02: Phase 3a — `GET /api/brain/graph` backend (`core/brain_graph.py`): entities + item/recording source nodes, co-occurrence (entity↔entity shared-source) + mention (source→entity) edges, `?focus=` ego graph, `limit` degree cap, honest empty graph. 5 tests; full suite 95.12% (gate ✓). Next: Phase 3b — the **web Brain section** (nav + route + tri-view Index/Wiki/Graph; Graph via `react-force-graph-2d` dynamic-import ssr:false, ego default, items toggle, no-fallback error/retry). Then verify in a real browser + surface to Mik.
- 2026-06-02: Phase 3b-1 — web Brain section scaffolding: **Brain** nav entry (Library group) + route + `BrainPanel` Index view (people/topics/projects chips from `GET /api/brain/graph`, sorted by degree, stats row, honest empty + error/retry), `getBrainGraph` client + `BrainGraph` types + CSS. 3 BrainPanel tests green; eslint + tsc clean (the 3 failing DashboardClient tests in the combined run are the pre-existing recording-delete timing ones). Next: Phase 3b-2 — the **Graph tab** (`react-force-graph-2d`, verify latest version first; dynamic ssr:false; color-by-kind, size-by-degree, hover-highlight-neighbors, click→ego focus; no-fallback error/retry), then the authenticated browser pass + surface to Mik.
- 2026-06-02: Phase 3b-2 — Brain **Graph tab** shipped (`react-force-graph-2d@1.29.1`, dynamic ssr:false, color/size/tooltip, click→ego-focus + Reset, show-sources toggle) + Index/Graph tab switcher; `BrainGraphView` + pure `buildForceGraph` (unit-tested). Verified: 7 unit tests + eslint + tsc + **`next build` (production) green** — confirms the dynamic-import/SSR integration. **NEXT (dedicated cycle): the live authenticated browser pass** — stand up the local stack (alembic upgrade dev DB → uvicorn → pnpm dev), register a user, seed a few entities+mentions, screenshot Brain Index + Graph (and `/ru`), then surface the Obsidian-style graph to Mik. THEN the Wiki tab (`GET /api/entities/{id}/page` + entity pages with backlinks).
- 2026-06-02: Phase 3b **VERIFIED LIVE** ✅ — fresh `waicomputer_brainviz` DB @ head → uvicorn :8000 → `next dev` :3000; registered brainviz@example.com; seeded 6 entities + 3 items + 9 mentions; Playwright login → Brain → **Index** (degree-sorted People/Topics/Projects chips ✓) + **Graph** (force-directed, 9 nodes / 19 links, color-by-kind, show-sources toggle ✓). Screenshots at `../wai-computer/brain-index.png` + `brain-graph.png`. **The flagship Brain visualization works end-to-end.** Local stack stopped after. Next options: Wiki tab + graph zoomToFit polish, or Phase 4 (Mac Brain port + comparison hardening).
- 2026-06-02: Phase 3c-backend — `GET /api/entities/{id}/page` (`build_entity_page`): source backlinks (item/recording titles + mention context) + co-occurrence related entities (ranked by shared sources) + mention_count; 404 for non-owned. 4 tests; full suite 95.13% (gate ✓). Next: **web Wiki tab** — `getEntityPage` client + a Wiki view in `BrainPanel` (entity infobox + Sources backlinks + Related nav; clicking an Index chip or graph node opens its wiki page). Then Phase 4 (Mac Brain) + graph zoomToFit polish.
- 2026-06-02: Phase 3c-web — **Wiki tab DONE → web Brain tri-view Index/Wiki/Graph complete** (both halves of the ask: Obsidian graph + wiki). `EntityWikiView` (infobox + Related chips that navigate the wiki + Sources backlinks with context, honest loading/error/empty) + `getEntityPage` client + `EntityPage` types; Index chips are now clickable → open the entity's wiki page. 8 web tests (3 EntityWikiView + BrainPanel chip→wiki) + eslint + tsc + `next build` ✓. **WEB BRAIN IS FEATURE-COMPLETE.** Next: Phase 4 (Mac Brain port — graph+wiki on the priority surface + comparison hardening); minor: live-verify Wiki in browser, graph zoomToFit.
- 2026-06-02: Phase 4a — Mac Brain KIT foundation: `BrainGraph` + `EntityPage` Codable models (Brain.swift) + `getBrainGraph`/`getEntityPage` on APIClient; +2 kit tests (swift test 475 ✓, 0 failures). Unblocks the Mac views. Next: Phase 4b — `MacBrainView` tabbed Index/Wiki/Graph (Index categorized list + Wiki tappable backlinks/related from `getEntityPage`), then 4c the SwiftUI Canvas force-graph; then comparison hardening + wire orphaned web power features.
- 2026-06-02: Phase 4b — `MacBrainView` rewritten to tabbed **Index/Wiki** (mirrors the verified web design): Index = degree-sorted entity rows by kind (tap→Wiki); Wiki = `getEntityPage` page with tappable Related + Sources backlinks; honest empty/error+Retry. xcodebuild ✓ (kit swift test 475 ✓). NOTE: macOS UI can't be screenshotted here (no Screen Recording perm) — Mik visually verifies on a real run; it's a faithful port of the browser-verified web Index/Wiki. Next: 4c Mac Graph (SwiftUI Canvas force-sim), then comparison hardening + orphaned web power features (unified search, comparison builder).
- 2026-06-02: Phase 4 (comparisons, part 1) — create-time correctness + recovery: `intent` column (migration 20260602_110000), de-dupe + ≥2-distinct guard (422), `POST /comparisons/{id}/rebuild` re-enqueuing with stored intent. 4 tests; migration scratch-validated; full suite 95.11% (gate ✓). Next: comparison build-side hardening (deleted-source filter + Celery retry + parallel rows w/ per-row tolerance + export md/csv + cell-edit PATCH), then orphaned web power features (search→/api/search/all, comparison builder UI), then 4c Mac Canvas graph (with Mik's visual review).
- 2026-06-02: Phase 4 (comparisons, part 2) — build-side robustness: deleted-source filter (`deleted_at IS NULL`; fail explicitly if <2 survive, else build survivors + record the exclusion — no silent row-drop) + Celery retry (max_retries=3, retry_backoff/jitter, `is_retryable_exception` guard). 4 tests (2 deleted-handling + 2 retry, mirroring the recording-task pattern); full suite 95.11% (gate ✓). PENDING comparison: parallel rows + per-row tolerance, export md/csv, cell-edit PATCH. Next: orphaned web power features (search→/api/search/all, comparison builder), then export/cell-edit, then 4c Mac graph.
- 2026-06-02: Phase 4 (comparisons, part 3) — export + cell-edit: `GET /comparisons/{id}/export?format=md|csv` (activates the dead `to_markdown` + a CSV writer; 409 if not ready) + `PATCH /comparisons/{id}` cell-edit (sets the dead `edited` flag; 422 unknown column / 404 unknown item; JSONB-safe reassign). 5 tests; full suite 95.11% (gate ✓). **Comparison hardening complete** (only parallel-rows perf deferred — retry already prevents data loss). Next: orphaned web power features (search → unified `/api/search/all`, comparison-builder UI), then 4c Mac Canvas graph (with Mik's visual review), then Phase 5 robustness + Phase 1 remainder.
- 2026-06-02: Phase 4 (web power features, part 1) — **unified search**: a new "Everything" search mode hitting `/api/search/all`, spanning recordings + items, rendered as a unified hit list (source-kind badge, snippet, score; open → recording detail or jump to Content). +1 DashboardClient test (Everything mode); tsc + eslint + `next build` ✓; no regressions (still only the 3 pre-existing recording-delete failures). Next: comparison-builder UI, connect-a-source UI, then 4c Mac Canvas graph + Phase 5.
- 2026-06-02: Phase 1 **MEDIA UPLOAD** (closes the last gap in goal #1 "add any material") — `POST /items/upload` now routes audio/video to the recording pipeline: streamed to a UUID staging path (never buffered whole) → new `app/tasks/media_import.py::import_uploaded_media_task` → `import_media_as_recording` (ffmpeg normalise + Deepgram), returning 202 {kind:recording,status:processing}; documents still return 201 Item. Recordings' own task is audio-only (no video normalise), so the items route owns the ffmpeg path. No-fallback: broker-down → 503 + staged file unlinked; staged file dropped on success/permanent-fail, kept on retryable; corrupt file → Sentry. 12 tests (6 route: video/audio→202+enqueue, empty→400, broker→503, too-large→413, unsupported→415; 6 task: success/permanent-fail/retryable/timeout lifecycle + `_import` both branches). `media_import.py` 100%; **full suite 1958 passed, coverage 95.15% (gate ✓)**; the 2 observability failures remain pre-existing/unrelated. Commit 3a3cb41f. Clients still cap their file pickers at documents (web `accept`, Mac `.fileImporter` types) — widening them to surface media upload + handle the 202 is the next client slice. Next: web/Mac media-picker widening, or comparison-builder UI, then 4c Mac graph + Phase 5.
- 2026-06-02: Phase 1 media upload — **WEB CLIENT now surfaces it** (was backend-only). `AddAnythingPanel` file-input + drag-drop `accept` widened to audio/video (mp3/wav/m4a/aac/ogg/opus/flac + mp4/mov/mkv/webm + `audio/*,video/*`); `uploadItem` returns a discriminated `UploadOutcome` — documents → `{kind:item}` (201, polled for summary as before), audio/video → `{kind:recording}` (202, no Item to poll) → panel shows "Transcribing — it'll appear in your recordings shortly" and skips the poll (discriminates on the 202 body having no `id`). 9 AddAnything tests (doc mock updated + new media-path test asserting no `getItem` poll); eslint + `next build` ✓; zero new tsc errors (34 pre-existing test-file errors unchanged). Commit 94416424. **Media upload is now end-to-end usable on web.** Next: Mac `.fileImporter` widening for media (the last client gap), or comparison-builder UI, then 4c Mac graph + Phase 5.
- 2026-06-02: Phase 1 media upload — **MAC CLIENT now surfaces it** (the last client gap; media upload now spans backend + web + Mac). WaiComputerKit `uploadItem` → `ItemUploadOutcome` (`.item(Item)` 201 / `.recording(status:)` 202, branches on status code — never decodes an Item from the 202 marker); MIME map + `MacContentFeedView.importTypes` widened to audio/video (`.audio`/`.movie` + mkv/webm/opus/ogg); `MacContentFeedViewModel.uploadFile` routes the outcome — media sets a localized `statusMessage` ("Transcribing — it'll appear in your recordings shortly." via OnboardingL10n) + skips the poll, docs unchanged; new info `statusBanner` in the view. Verify: kit `swift test` **476 passed** (doc test unwraps `.item`; new test asserts 202→`.recording("processing")`); macOS **xcodebuild BUILD SUCCEEDED**. Mac UI is a faithful port of the browser-verified web flow (not screenshot-verifiable here — Mik visual-checks). Commit 0ceb3d0d. **"Add any material" (incl. audio/video) is now complete on all clients.** Next: comparison-builder UI (web, fully testable), or 4c Mac Canvas graph (needs Mik's eyes), then Phase 5.
- 2026-06-02: loops + hooks REMOVED at Mik's request (cron caae7a66 deleted; hook registrations already stripped from `~/.claude-mik/settings.json` + pause flags set + `continue-voice-phases.sh` gone — nothing re-fires on stop). Working straight through in-session from here.
- 2026-06-02: Phase 4 (web power features, part 2) — **comparison-builder UI COMPLETE end-to-end** (web reaches Mac parity; was the main remaining "power feature"). Two slices: (1) `ComparisonView` (commit 8d77af26) — modal that polls `getComparison` until terminal then renders the induced columns as an Item × attributes table; honest building/failed(+schema_rationale)/no-columns/load-error states; null cells → "—"; backdrop+Escape close; latest-ref so polling survives parent re-renders; +4 tests. (2) `ItemsFeed` multi-select (commit 3088711c) — per-row selection checkbox (sibling to the detail button, divider moved to the row wrapper), a "Compare N" header action at ≥2 selected → `createComparison({item_ids})` → opens the modal + clears selection; errors via onError; selection clears on filter change; +1 test (8 existing still green). eslint + `next build` ✓ on both. Next: connect-a-source UI (check if Settings → MCP already covers it), or 4c Mac Canvas graph (needs Mik's eyes), then Phase 5 (yt-dlp opt-in, governance, OCR).
